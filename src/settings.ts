export enum Color {
  BLACK = "#000",
  GREEN = "#0f0",
  BLUE = "#00f",
  RED = "rgb(255, 0, 0)",
  HIGLITE_BOT = "rgb(162, 0, 0)",
  HIGLITE_TOP = "rgb(240, 65, 65)",
  WHITE = "rgb(255, 255, 255)",
  GOLD = "rgb(232, 180, 65)",
}

export enum Events {
  KEYUP = "keyup",
  KEYDOWN = "keydown",
}

export enum KeyCode {
  LEFT_ARROW = 37,
  RIGHT_ARROW = 39,
}

export enum Direction {
  LEFT,
  RIGHT,
  NONE,
}
