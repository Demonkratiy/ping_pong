export enum Color {
  BLACK = "#000",
  GREEN = "#0f0",
  BLUE = "#00f",
}

export enum Events {
  KEYUP = "keyup",
  KEYDOWN = "keydown",
}

export enum KeyCode {
  LEFT_ARROW = 37,
  RIGHT_ARROW = 39,
}

export enum Direction {
  LEFT,
  RIGHT,
  NONE,
}
